version: "2.1"

services:
  # Gymnasium: Commented out to make a lighter devstack.
  # discovery:
  #     volumes:
  #     - ${DEVSTACK_WORKSPACE}/edx-themes:/edx/app/edx-themes:cached
  # ecommerce:
  #     volumes:
  #     - ${DEVSTACK_WORKSPACE}/edx-themes:/edx/app/edx-themes:cached
  lms:
      volumes:
      - ${DEVSTACK_WORKSPACE}/edx-themes:/edx/app/edx-themes:cached
  studio:
      volumes:
      - ${DEVSTACK_WORKSPACE}/edx-themes:/edx/app/edx-themes:cached
